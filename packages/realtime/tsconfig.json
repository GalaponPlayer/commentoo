{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    // The browser client (client.ts) uses WebSocket/fetch. DOM types are scoped
    // to this package; consumers of the protocol-only entry never load them.
    "lib": ["ES2022", "DOM"]
  },
  "include": ["src"]
}
